

![[Pasted image 20251008190929.png]]

in the above payload the img tag is used to display the image in the html language , The src is the actual link where the url of the website is stored and it will redirect . if the url is try to render there will be the error , the onerror is that when the error occur the script inside the onerror will executed and the it will send the cookie to the attacker.com


---> 'accesskey='x'onclick='alert(1)

the above payload says that , it define the access key ctrl+shift+x , when the key is pressed the element is activated and then alert(1) is pop-up using the onclick event handler 





